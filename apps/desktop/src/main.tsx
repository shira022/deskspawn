import React from "react";
import ReactDOM from "react-dom/client";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <div>
      <h1>DeskSpawn Desktop</h1>
      <p>Desktop version — Tauri backend with shared UI packages.</p>
    </div>
  </React.StrictMode>,
);
